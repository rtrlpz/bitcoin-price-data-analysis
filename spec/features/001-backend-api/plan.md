# 001 — Implementation Plan

1. Create `backend/` package structure
2. Install fastapi + uvicorn
3. Create `backend/api/main.py` with CORS configuration
4. Create each route file, one domain per file
5. Create WebSocket placeholder
6. Create `backend/requirements-backend.txt`
7. Test each endpoint with uvicorn + curl/httpie
